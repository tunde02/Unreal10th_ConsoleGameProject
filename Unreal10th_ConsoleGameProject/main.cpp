#include <iostream>
#include <windows.h>
#include <vector>
#include <string>
#include "GameEngine.h"

int main()
{
    
    GameEngine::GetInstance().Run();
    GameEngine::GetInstance().Run();
    GameEngine::GetInstance().Run();
}
